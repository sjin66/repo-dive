{"id":"context.budgeted_complete_evidence","category":"context","mode":"specification","prompt":"Pack grounded implementation evidence for a coding agent under an explicit token budget.","expected_behavior":"The command returns deterministic complete Evidence items with stable IDs, source locations, symbols, source text, scores, and reasons; estimated usage stays within budget and the repository index remains byte-for-byte unchanged.","command":"repo-dive context tests/fixtures/index_repo greet --token-budget 1200 --max-results 5 --format json","assertions":["exit code is 0","estimated_tokens is at most token_budget","each Evidence item has a complete inclusive line range","each Evidence item has a stable evidence_id","repeated output is byte equivalent",".repo-dive digest is unchanged"]}
{"id":"context.tiny_budget","category":"context","mode":"specification","prompt":"Request repository context with a positive budget too small for any complete Evidence item.","expected_behavior":"The command succeeds with no items, reports truncated true and a positive budget exclusion count, and never emits a partial source range.","command":"repo-dive context tests/fixtures/index_repo greet --token-budget 1 --format json","assertions":["exit code is 0","items is empty","truncated is true","excluded.budget is positive","estimated_tokens is at most token_budget"]}
