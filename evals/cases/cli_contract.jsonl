{"id":"cli.help.available","category":"cli","prompt":"Discover how to invoke repo-dive without interactive input.","expected_behavior":"The command exits successfully and describes a local repository evidence CLI.","command":"repo-dive --help","assertions":["exit code is 0","stdout contains local repository evidence"]}
{"id":"cli.version.stable","category":"cli","prompt":"Read the installed repo-dive version.","expected_behavior":"The command exits successfully and prints one stable version line.","command":"repo-dive --version","assertions":["exit code is 0","stdout is repo-dive 0.1.0"]}
{"id":"io.json.stdout_isolated","category":"io_contract","prompt":"Request a future functional command using JSON output.","expected_behavior":"Stdout contains exactly one complete JSON document while diagnostics are emitted only on stderr.","assertions":["no ANSI sequences in stdout","JSON ends with one newline"]}
{"id":"io.diagnostics.stderr","category":"io_contract","prompt":"Observe progress from a future long-running repository operation.","expected_behavior":"Progress and warnings are emitted on stderr and never corrupt a structured stdout result.","assertions":["stdout remains machine parseable"]}
{"id":"artifact.wiki.stable_path","category":"artifact_contract","prompt":"Locate the current generated repository wiki.","expected_behavior":"The current assembled Markdown is stored at <repository>/.repo-dive/wiki.md.","assertions":["the path is stable across regenerations","failed assembly preserves the previous file"]}
