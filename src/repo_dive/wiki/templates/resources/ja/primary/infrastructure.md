<!-- repo-dive:contribution=infrastructure; locale=ja; -->
<!-- repo-dive:page=resource_topology_page; order=1; cardinality=1; shape=heading,paragraph,table,list; purpose=管理資源と依存を記録する; evidence=必須の根拠は declaration、module、stack、行番号; constraints=identity と依存方向を正確にする; --> **{{repo_dive:resource_topology_page}}**
<!-- repo-dive:page=environments_state_page; order=2; cardinality=1; shape=heading,paragraph,table,list; purpose=environment、config、state を説明する; evidence=必須の根拠は backend、variable、workspace、行番号; constraints=secret 値を出さない; --> **{{repo_dive:environments_state_page}}**
<!-- repo-dive:page=network_security_page; order=3; cardinality=1; shape=heading,paragraph,table,list; purpose=network と identity 境界を説明する; evidence=必須の根拠は policy、route、role、行番号; constraints=実際の強制だけを書く; --> **{{repo_dive:network_security_page}}**
<!-- repo-dive:page=infrastructure_change_page; order=4; cardinality=1; shape=heading,paragraph,list,code_block; purpose=plan、apply、change control を説明する; evidence=必須の根拠は automation、command、pipeline、行番号; constraints=順序と rollback を根拠化する; --> **{{repo_dive:infrastructure_change_page}}**
<!-- repo-dive:page=observability_recovery_page; order=5; cardinality=1; shape=heading,paragraph,list,code_block; purpose=監視と復旧を説明する; evidence=必須の根拠は metric、log、alert、backup、行番号; constraints=自動と手動を分ける; --> **{{repo_dive:observability_recovery_page}}**
<!-- repo-dive:page=infrastructure_testing_page; order=6; cardinality=1; shape=heading,paragraph,list,code_block; purpose=検証と test を説明する; evidence=必須の根拠は static check、policy test、command、行番号; constraints=範囲を誇張しない; --> **{{repo_dive:infrastructure_testing_page}}**
